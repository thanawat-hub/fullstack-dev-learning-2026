how to run this js via node
```
node {filename.js}
```
แปลว่า เราไม่ต้องไปเชื่อมกับ html ที่เรียก script แล้ว เรียก node แล้วดูผ่าน console ได้เลย 
และต้องรู้บางอย่าง เช่นใน node ไม่มี prompt เหมือนใน browser